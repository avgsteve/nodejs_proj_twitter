include ../mixins/mixins

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        //- 透過後端 rootRoutes.js 給予 pageTitle 變數的資料內容
        title #{pageTitle}
        //- 裁剪圖片用的 cropper CSS
        link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.9/cropper.min.css')
        //- 
        link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css', integrity='sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh', crossorigin='anonymous')
        //- Bootstrap icon
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.0/font/bootstrap-icons.css">

        link(rel="stylesheet", href="/css/main.css")

        //- 暫時使用下面的方式讓瀏覽器尋找 favicon.ico
        <link rel="shortcut icon" href="#" /> 
    body
        script.
            var userLoggedIn = !{userLoggedInJs}; // unescaped code (user data obj) from  
            //- console.log('前端收到的資料 userLoggedIn: ', userLoggedIn);
        .wrapper
            .row
                nav.col-2.nav-col.mobil-nav-align-center.active
                    .fixed-nav
                        .scrollable
                            a.blue(href="/")
                                i.fas.fa-dove
                            a(href="/")
                                i.fas.fa-home
                            a(href="/search")
                                i.fas.fa-search
                            a(href="/notifications")
                                i.fas.fa-bell
                                span#notificationBadge
                            a(href="/messages/list")
                                i.fas.fa-envelope
                                span#messagesBadge
                            a(href="/profile")
                                i.fas.fa-user
                            a(href="/me")
                                i.bi.bi-gear-fill                                
                            a(href="/logout")
                                i.fas.fa-sign-out-alt

                //- 每個 function的主要頁面
                .mainSectionContainer.col-10.col-md-8.col-lg-6

                    //- 頁面的標題框架 + 自訂按鈕 (headerButton)
                    .titleContainer
                        h1 #{pageTitle}
                        block headerButton

                    block content

                //- .d-none.d-md-block.col-md-2.col-lg-4
                .floatingMenuButtonContainer

                    i.bi.bi-menu-app.sideMenuToggleBtn


        #popupNoticeContainer

        script(src='https://kit.fontawesome.com/85404227e2.js' crossorigin='anonymous')
        script(src='https://code.jquery.com/jquery-3.5.1.min.js', integrity='sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=', crossorigin='anonymous')

        script(
        src="https://code.jquery.com/ui/1.12.1/jquery-ui.min.js"
        integrity="sha256-VazP97ZCwtekAsvgPBSUwPFKdrwD3unUfSGVYrahUqU="
        crossorigin="anonymous")

        //- 裁剪圖片用的 cropper CSS
        script(src='https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.9/cropper.min.js')
        
        script(src='https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js', integrity='sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo', crossorigin='anonymous')
        script(src='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js', integrity='sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6', crossorigin='anonymous')
        script(src='https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.3.0/socket.io.js')

        block scripts

        //- script(src="/js/clientSocket.js")
        script(src="/js/bundle.js")
        

